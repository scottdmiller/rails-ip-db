<%= form_with(model: firewall, local: true) do |form| %>
  <% if firewall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(firewall.errors.count, "error") %> prohibited this firewall from being saved:</h2>

      <ul>
      <% firewall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_area :source, id: :firewall_source %>
  </div>

  <div class="field">
    <%= form.label :port %>
    <%= form.number_field :port, id: :firewall_port %>
  </div>

  <div class="field">
    <%= form.label :destination %>
    <%= form.text_area :destination, id: :firewall_destination %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
